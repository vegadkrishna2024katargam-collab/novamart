# TODO

- [x] Update `src/components/ProductCard.jsx` to show additional listing details and thumbnails; ensure card click navigates correctly to `/product/:id`.
- [x] Update `src/data/demoData.js` products to include realistic Amazon-like fields: `images` (multiple), `brand`, `countInStock` (or `stock`), `numReviews` (or `reviewCount`), and richer `description` (plus optional `specs`).

- [ ] Run the app and verify: clicking cards opens `ProductDetailsPage` and multiple images render.


